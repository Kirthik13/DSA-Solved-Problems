#include<bits/stdc++.h>
using namespace std;

int main(){
    //store the occurrences in the result vector
    vector<int> result = {};
    int idx{};
    while(big.find(small,idx+1)<big.size()){
            idx=big.find(small,idx+1);
            result.push_back(idx);
            
            
    }
    

}
